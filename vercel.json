{
  "buildCommand": "cd study-tracker && npm install && npm run build",
  "outputDirectory": "study-tracker/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
